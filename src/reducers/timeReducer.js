// import { SET_TIME } from '../actions';

// const initialStateAnswer = {
//   count: 0,
// };

// function timeReducer(state = initialStateAnswer, action) {
//   switch (action.type) {
//     case SET_TIME:
//       // return { ...state, count: state.count + action.count };
//       return {
//         ...state,
//         count:
//           state.count + action.count > 30 ? 30 : state.count + action.count,
//       };
//     default:
//       return state;
//   }
// }

// export default timeReducer;
